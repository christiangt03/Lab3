name: Matrix Build Enterprise
 
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
 
permissions:
  contents: read
 
jobs:
  build:
    name: build-${{ matrix.os }}-node${{ matrix.node }}-${{ matrix.mode }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        node: [18, 20]
        mode: [debug, release]
        exclude:
          # Incompatible: nuestra base Windows no soporta Node 18
          - os: windows-latest
            node: 18
          # Innecesaria: no compilamos en debug sobre Windows en CI
          - os: windows-latest
            mode: debug
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
 
      - name: Setup Node.js ${{ matrix.node }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
 
      - name: Show matrix variables
        shell: bash
        run: |
          echo "OS:    ${{ matrix.os }}"
          echo "Node:  ${{ matrix.node }}"
          echo "Mode:  ${{ matrix.mode }}"
          node --version
 
      - name: Simulated build
        shell: bash
        run: |
          if [ "${{ matrix.mode }}" = "debug" ]; then
            echo "Building with DEBUG flags (verbose, no minify)"
          else
            echo "Building with RELEASE flags (optimize, minify)"
          fi
          sleep 3
          echo "Build OK"
